import * as React from 'react';
import { shallow } from 'enzyme';

import Tooltip from './ToolTip';

describe('Tooltip test', () => {
  it('asdsa', () => {
    const wrapper = shallow(<Tooltip name ="asdasd"/>);
  })
});